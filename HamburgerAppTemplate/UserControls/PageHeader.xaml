<UserControl
    x:Class="HamburgerAppTemplate.UserControls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HamburgerAppTemplate.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="400">

    <Grid >
         <CommandBar Background="{StaticResource MyChromeBackgroundBrush}" x:Name="myCommandBar" VerticalContentAlignment="Center">
            <CommandBar.Content>
                <TextBlock x:Name="titleText" Text="{x:Bind Title}" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
            </CommandBar.Content>
        </CommandBar>
    </Grid>
</UserControl>
